# 实验 4-3 实验笔记：多模态三范式对比

> 配套《深入理解 AI Agent》第 4 章 · 实验 4-3
> 实验目录：`E:\python-study\ai-agent-book\chapter4\multimodal-agent`
> 运行日期：2026-09-22

---

## 一、实验要回答的问题

同一个多模态文件、同一个问题，交给三条不同的输入路径，结果差在哪？

书里给的预期是：
- **原生多模态**：直接"看"图，保留视觉布局，能力上限最高，但 token 多、模型支持有限
- **提取为文本**：先转文字再推理，通用、可缓存、省 token，但**丢视觉细节**
- **工具化分析**：可追问、按需深挖，但要多轮 API 调用

关键问题是：**"丢视觉细节"到底丢什么？值不值得为此付两次调用的代价？**

---

## 二、实验素材（设计得很讲究）

`create_sample.py` 离线生成的样例（无需 API Key）：

| 文件 | 内容 |
|---|---|
| `test_files/sample_chart.png` | 柱状图，Q1–Q4 营收 |
| `test_files/sample_report.pdf` | 文字说明 + 嵌入同一张柱状图 |

**核心设计**：精确数字（Q1=$120M / Q2=$150M / Q3=$95M / Q4=$180M）**只标注在柱子顶端**。
PDF 正文只给定性描述：

> "Overall the year showed healthy growth, with a mid-year dip followed by a
> strong recovery in the final quarter."

正文里**一个精确数字都没有**。这样"提取为文本"若只转正文，就必然丢数据——对照组是设计好的，不是碰运气。

**正确答案：Q4，$180M。**

---

## 三、环境与配置

| 项目 | 值 |
|---|---|
| conda 环境 | `agentbook`（Python 3.12.14） |
| 模型 | 火山方舟 `Doubao-Seed-2.1-pro`（视觉理解） |
| 接入点 ID | `ep-20260922203225-zzs9k` |
| `ARK_MODEL` | `doubao-seed-2-1-pro-260915`（模型名直调） |
| Base URL | `https://ark.cn-beijing.volces.com/api/v3` |
| 配置文件 | `chapter4/multimodal-agent/.env` |

### 配置要点

1. **DeepSeek 跑不了这个实验**——它不支持图像输入，三条路径全断。这是能力缺口不是配置问题。
2. **火山方舟两种调用方式都通**：模型名直调（`doubao-seed-2-1-pro-260915`）和接入点 ID（`ep-...`），实测均可。
3. **别选错模型**：`Doubao-Seedance-2.5` 是视频生成模型、`Doubao-Seedream-5.0-pro` 是图像生成模型，都不能做图像理解问答。要选 `Seed-2.1` 系列里带视觉能力的。
4. **`--model` 参数填别名**：`doubao-1.6`（对应 `config.py` 里 `self.models` 的键名），不是模型全名。

### 运行命令

```bash
conda activate agentbook
cd /d E:\python-study\ai-agent-book\chapter4\multimodal-agent

python demo.py --file test_files/sample_chart.png ^
  --query "Which quarter had the highest revenue, and what was the exact value?" ^
  --model doubao-1.6 --skip-model-comparison
```

`--skip-model-comparison` 只跑三范式对比，跳过跨模型对比（省钱）。

---

## 四、实测结果

三个范式**全部答对**：`Q4 / $180M`。

| 范式 | 答案 | 中间产物 | 调用次数 |
|---|---|---|---|
| **1. 原生多模态** | Q4 / $180M ✅ | 无，直接读图 | 1 次 |
| **2. 提取为文本** | Q4 / $180M ✅ | 生成约 2400 字符的图描述，再基于描述回答 | 2 次 |
| **3. 提取 + 工具** | Q4 / $180M ✅ | 同上，另可对同一文件追问 | 2 次 + 追问时 1 次 |

### 各范式的输出特征

**原生多模态**（最简洁）：
```
Looking at the bar chart:
**Q4 (the fourth quarter of 2024)** had the highest revenue, at exactly $180 million ($180M).
```
没有任何中间描述，一次调用给出答案。

**提取为文本**（中间产物极长）：
- 生成的描述包含：图表类型、标题、坐标轴范围与刻度、每根柱子的**颜色/高度/精确数值**、趋势分析、甚至**全年合计 $545M**
- 描述里把 `$120M/$150M/$95M/$180M` 全写进去了，所以最终也答对
- **代价**：多一次调用，且这段描述本身占了大量 token

**提取 + 工具**（独有能力）：
```
Follow-up question: What colors are dominant in the image at test_files/sample_chart.png?
[Tool: analyze_image]
```
它会**自动提出后续问题**并调用 `analyze_image` 工具——这是前两个范式做不到的：
前面只把图"转成描述"了，描述一旦生成就固定；工具化保留了"回到原文件重新看"的能力。

---

## 五、结论：和书里的预期不完全一样

### 反直觉点 1：这次"提取为文本"没丢数据

书里预期提取路径会丢视觉细节。但实测**没丢**——因为豆包在生成描述时，把柱子上的数字全读出来了，连颜色和趋势都描述了。

**这说明"丢不丢"取决于描述生成本身的质量**，不取决于"是不是走了提取这条路"。样例是一张**干净、字大、无遮挡**的柱状图，对视觉模型来说太容易了。

### 反直觉点 2：真正该看的不是"谁答对"，而是成本结构

| 指标 | 原生 | 提取为文本 |
|---|---|---|
| 调用次数 | 1 | 2 |
| prompt token（实测） | 1341 | 描述本身约 2400 字符 + 第二轮的描述 token |
| 中间产物 | 无 | 约 2400 字符描述 |

提取路径的设计初衷是"**用便宜的文本模型替代昂贵的视觉模型**"。但本次实验里两条路用的**都是同一个视觉模型**，所以这个优势体现不出来，代价却翻倍了。

**想复现这个优势，得换场景**：用视觉模型生成一次描述并缓存，之后所有问答都走纯文本模型。

### 反直觉点 3：真要看到"提取丢信息"，需要极端素材

书里 README 自己写了：「示例正确不代表任意扫描件或复杂表格都适用」。

想看提取路径翻车，得用这类素材：
- **复杂表格**（多级表头、合并单元格）——OCR 容易串行
- **文字很小的扫描件**——识别率下降
- **依赖空间布局的图**（流程图、拓扑图）——转文本必丢关系
- **手写内容**

干净的柱状图不在这个列表里。

---

## 六、三条路径怎么选

| 场景 | 选哪条 | 理由 |
|---|---|---|
| 复杂混排文档、要看布局 | **原生** | 唯一能保留空间关系的方式 |
| 纯文字文档、批量处理 | **提取为文本** | 一次转写可缓存，后续用文本模型反复问 |
| 需要多轮追问同一文件 | **提取 + 工具** | 工具保留回到原文件的能力 |
| 偶发的一次性问答 | **原生** | 少一次调用，最省事 |

**关键判断依据**：问自己「**这个文件里的信息，是否只靠文字就能完整表达**」。
- 是 → 提取为文本够用，而且更便宜（前提是能缓存）
- 否（依赖布局/颜色/空间关系）→ 必须原生或工具化

---

## 七、遇到的问题与解决

### 1. `No module named 'agentbook'`

**原因**：`agentbook` 是 `pip install -e` 的可编辑安装，源码目录从 `E:\ai-agent-book\` 搬到了 `E:\python-study\ai-agent-book\`，editable finder 里的路径没更新。

**诊断过程**：
- `__editable___agentbook_0_1_0_finder.py` 里的 `MAPPING` 仍指向旧路径
- 改完路径还是不行——查 `sys.meta_path` 发现 `_EditableFinder` 根本没注册
- 真正原因：`.pth` 注册文件也被删了

**最终解法**：放弃 editable 机制，直接把 `agentbook/` 目录拷进 site-packages。简单、不会因目录移动而失效。

### 2. pip 安装被安全守卫中断（重要教训）

**现象**：`pip install -r requirements.txt` 在 `Installing collected packages` 阶段被杀，退出码 2，报 `Safe-Delete` 批量删除守卫。

**后果**：pip 已经删了旧目录但没写回新文件，导致 **85 个包**残缺（`idna.core`、`anyio.to_thread`、`click.command` 等子模块消失）。

**错误的做法**（我犯的）：
- 在**唯一的可用环境**上直接跑 `pip install -r`
- 用 `pip install --force-reinstall` 试图修复——同样触发守卫，问题扩大

**有效的做法**：
```python
# 1. 用 pip download 把 wheel 下到干净目录（download 阶段不触发守卫）
pip download --no-deps --dest <wheelhouse> <packages>

# 2. 用 Python 的 zipfile 直接解压覆盖到 site-packages
#    覆盖写入不触发删除守卫
with zipfile.ZipFile(whl) as z:
    z.extractall(site_packages)
```

**结论**：在这个环境里，**不要用 pip 做安装/升级**，改用「下载 wheel + 手动解压」。加 `--no-deps` 也能规避大部分守卫触发。

---

## 八、下一步可以做的

1. **换个素材测提取路径的极限**：找一张复杂表格或有手写内容的扫描件，同一命令跑一遍，对比三条路径的答案准确率
2. **量化 token 成本**：目前只测了原生模式的 prompt token（1341），可以给三个范式都加上 token 统计，把"代价翻倍"变成具体数字
3. **试 PDF 素材**：`sample_report.pdf` 已生成但没跑——PDF 原生模式 README 说只有 Gemini 支持，豆包可能走不了，值得验证一下
4. **`--tools` 开关的作用**：第三段跑了 `extract + tools`，可以关掉 tools 单独跑一次，看追问能力消失后有什么不同

---

## 附：产出文件

| 文件 | 说明 |
|---|---|
| `result_4-3.txt` | 完整对比记录（4147 B） |
| `test_files/sample_chart.png` | 样例图表（35160 B） |
| `test_files/sample_report.pdf` | 样例报告（47826 B） |
| `.env` | 环境配置（含 API Key，注意不要外传） |
