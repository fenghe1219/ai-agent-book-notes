# 实验 2-10：上下文压缩策略对照 —— 实验笔记

日期：2026-09-20 深夜 ~ 09-21 凌晨

---

## 一、实验要解决的问题

第一章实验 1-1 里，我们观察到 Agent 的输入 tokens 逐轮膨胀（748 → 1,298 → 1,444）。
上下文只涨不压，迟早撞上窗口上限。**实验 2-10 就是研究怎么把上下文压下来，同时不毁掉能力。**

- 任务：调研 OpenAI 联合创始人的当前任职（需要多轮搜索 + 读长网页）
- 变量：6 种上下文压缩策略
- 预算：`CONTEXT_WINDOW_SIZE = 128,000` tokens
- 模型：DeepSeek `deepseek-v4-flash`

---

## 二、环境与配置（复用第一章）

目录：`E:\ai-agent-book\chapter2\context-compression\`（18 个文件）

```
conda activate agentbook
cd /d E:\ai-agent-book\chapter2\context-compression
pip install -r requirements.txt
copy ..\..\chapter1\context\.env .env
notepad .env          # 加 LLM_PROVIDER=deepseek + MODEL_NAME=deepseek-v4-flash
python main.py
```

**三个配置坑（都踩过）：**

| 坑 | 现象 | 解法 |
|----|------|------|
| 复制来的 .env 里残留 `LLM_PROVIDER=doubao` | 加的新行没生效，旧值赢 | 搜出所有 `LLM_PROVIDER`，删掉非 deepseek 的行 |
| `MODEL_NAME` 缺失时默认用 `kimi-k3` | 模型名发给 DeepSeek 会被拒 | 显式加 `MODEL_NAME=deepseek-v4-flash` |
| 同一变量多行 | 后面的覆盖前面的 | 排查时按变量名逐行核对 |

**注意**：`quickstart.py` 的环境检查写死了只认 kimi/千问，会误报缺 key，**别用它**；用 `main.py`（走 agentbook 的 resolve_backend，认 deepseek）。

**SERPER_API_KEY 不填也能跑**：搜索工具自动改用模拟/缓存数据。代价是结论时效性差、部分网页请求超时，模型会自己在答案里标注“数据可能过时、未能核实”。若要真实数据，去 serper.dev 免费注册。

---

## 三、六种策略

| 编号 | 策略 | 机制 |
|------|------|------|
| 1 | No Compression | 不压缩（菜单自标 “expected to fail with large contexts”） |
| 2 | 逐页摘要 | 每个网页各自摘一段，再拼接 |
| 3 | 合并摘要 | 所有页面拼成一大坨，整体摘一次 |
| 4 | 上下文感知摘要 | 摘要时知道任务目标，优先保留相关内容 |
| 5 | 上下文感知 + 引用 | 在 4 的基础上保留来源标注 |
| 6 | 滑动窗口 | 只在接近上下文上限时才压缩 |

---

## 四、实测数据（4 轮）

| 策略 | 结果 | 工具调用 | 耗时 | 总 tokens | Prompt | 压缩比 | 节省字符 | 溢出 |
|------|------|---------|------|-----------|--------|--------|---------|------|
| **5 引用** | 成功 | 41 | 49.9s | 83,966 | 78,190 | 138.0% | −8,708 | 0 |
| **6 窗口** | 成功 | 94 | 163.7s | 307,087 | 289,529 | 145.9% | −18,252 | 0 |
| **4 感知摘要** | **失败** | 81 | 369.9s | 399,851 | 391,537 | **21.7%** | **+43,688** | 0 |
| **（某策略）** | **失败** | 138 | **1222s** | **6,436,583** | 6,414,852 | 40.8% | +23,884 | **26** |

（另有一轮因 `Connection error` 在 0 次工具调用时即失败，无数据。）

---

## 五、结论：压缩率越高，能力未必越好

**四种典型失败形态，一轮实验全见齐了：**

1. **膨胀但可用**（策略 5，138%）
   全保留原文 + 引用标注，上下文越滚越大，但答案信息完整、可追溯。**信息密集的调研任务上，这种“宁可膨胀”的做法反而最靠得住。**

2. **该压不压**（策略 6，145.9%）
   只在接近上限才压缩，而 128K 预算下**条件始终没触发**，等于完全不压缩。烧掉 30 万 tokens，行为与策略 1 无异。

3. **压狠了崩**（策略 4，21.7%）
   **压缩最成功，任务最失败**——压掉近 80% 的同时把回答所需的关键细节一起压没了，于是反复重查（81 次调用）、最终没得出结论。这轮是全场最有价值的“成功失败”。

4. **彻底失控**（最后一轮，640 万 tokens，26 次溢出）
   上下文撞墙 26 次 → 信息丢失 → 重查 → 上下文再涨 → 再溢出，形成死亡螺旋：138 次工具调用、20 分钟、任务仍失败。

**核心洞察**：
> **压缩率不是 KPI。省 token 和保能力是一对矛盾。**
> 压缩的价值不在“压得有多小”，而在“压掉的是不是噪声”。策略 4 之所以失败，是因为它按“与任务相关”打分压缩——但调研任务的“相关”事先并不知道，被压掉的恰恰可能是后来才需要的那条线索。

---

## 六、工程提醒

- **成本**：失控那轮 640 万 tokens，顶前面几轮总和。交互模式连跑多轮时留意消耗。
- **读日志只看三处**：`Context Overflows`、`Compression Ratio`、`Space Saved`。前两个为 0/正数才健康。
- 结果不落盘（`main.py` 交互模式只打印统计），**要留证据得自己截图**。

---

## 七、和第一章的联系

| | 实验 1-1 | 实验 2-10 |
|---|---|---|
| 变量 | 上下文**缺**一块 | 上下文**压**一块 |
| 失败形态 | no_history 失忆循环（50 次调用） | 策略 4 压掉关键信息（81 次调用） |
| 共同结论 | 上下文决定 Agent 能力上限 | 上下文的**质量**比**数量**更关键 |

两次实验指向同一件事：**Agent 的瓶颈不在模型，在上下文管理。** 这也是第 2 章存在的理由。
