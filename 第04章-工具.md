# 第 4 章 工具

来源：《深入理解 AI Agent：设计原理与工程实践》（bojieli/ai-agent-book，Apache-2.0）
原文：book/chapter4.md ｜ 配套实验：chapter4/ 共 5 个 ｜ 难度：🔵 进阶级

## 一、本章一句话主旨

工具是 Agent 的「双手」：用统一的 ACI 设计原则（通用优于专用、描述重边界、参数保真）表达能力，借 MCP 与 Skill Hub 分发，再靠层次化组织与主动发现应对「工具爆炸」；感知工具控信息量，执行工具控风险，协作工具做专业化分工。

## 二、核心概念与公式

| 概念 | 说明 |
|---|---|
| 五类工具 | 感知 / 执行 / 协作 / 事件触发 / 用户沟通（前三类 Agent 主动调用，后两类依赖异步运行时放第 6 章） |
| ACI (Agent-Computer Interface) | 工具应对应 Agent 的目标而非底层 API；对标 HCI，让工具对 Agent 而非人友好 |
| 专用工具 vs 通用执行器 vs Skill | 结构化函数调用 / bash·code_interpreter / 自然语言 SKILL.md 目录 |
| 默认取向 | 通用工具优于专用工具，除非有明确安全、权限或性能理由 |
| 静默输入转换 / 静默参数注入 | 工具偷偷「修正」或「附加」参数，制造模型无法诊断的系统性偏差 |
| MCP (Model Context Protocol) | Anthropic 2024 末发布的开放标准，统一模型与工具/数据源通信（客户端-服务器、JSON Schema、stdio/Streamable HTTP、tools/resources/prompts 三类原语） |
| Skill Hub | 用注册表（registry，如 skills.sh 的 npx skills add）分发 SKILL.md 文件夹，常驻仅 name+description |
| 按需加载 / 主动工具发现 | 只暴露索引、检索式预筛选、MCP-Zero 语义路由 |
| Proposer-Reviewer / Sidecar | 异源模型双签审批 / 与主模型流式并行、门控单次调用的轻量安全校验 |
| 幂等性 | 同一操作执行一次与多次对外部世界影响相同，可安全重试 |

## 三、分节精读

### 4.1 工具的分类

从两个特征审视五类工具：调用方向（谁发起）与作用对象（作用于什么）——两列不构成交叉分类，每类在作用对象上有专属取值。表 4-1：

| 工具类别 | 调用方向 | 作用对象 |
|---|---|---|
| 感知工具 | Agent 主动调用 | 获取信息 |
| 执行工具 | Agent 主动调用 | 改变世界 |
| 协作工具 | Agent 主动调用 | 驱动其他 Agent 或人类 |
| 用户沟通工具 | Agent 主动调用 | 向用户传递信息 |
| 事件触发工具 | Agent 注册、外部触发 | 驱动 Agent 开始执行 |

- 感知工具：主动获取信息（web_search、knowledge_base_search、fetch_url、find_file、grep_file、read_file）；设计关键在控制输出信息量防上下文爆炸。
- 执行工具：改变外部世界（shell_exec、code_interpreter、write_file、edit_file、send_email）；错误代价极高，安全约束是核心。
- 协作工具：与其他 Agent/人类协作（spawn_subagent、send_message_to_subagent、cancel_subagent、list_agents）；用于并行任务或不同模型/工具/上下文。
- 用户沟通工具：主动传递信息（reply_to_user、send_card_to_user、send_user_notification）；跨渠道异步触达需显式工具调用。
- 事件触发工具：外部世界驱动（set_timer、monitor_shell、connect_channel）；分注册（Agent 主动声明关心什么事件）与触发（外部异步回调唤醒）两个时刻。

### 4.2 工具设计的通用原则

核心论点：早期是 API 封装，粒度过细；成熟思路是 ACI——工具对应目标而非底层操作。

- 能力的表达形式：专用工具、通用执行器与 Skill。谱系示例「部署应用」：可做成 deploy_app 专用工具、拆成构建/打包/部署三个细工具、或只写一份 Skill 文档用 bash 执行（npm run build → docker build → kubectl apply）。本节问的是形态不是数量：形态决定每条能力常驻多少 token、参数怎么传、谁能改；披露策略决定多少条同时摆在面前。
- 默认取向：通用工具优于专用工具，除非有明确安全/权限/性能理由——如与其给四则运算计算器不如给 code_interpreter（装好 sympy/numpy/pandas），利用 LLM 的代码能力而非限制它。即使专用工具也偏向整合（如统一 read_document 用 file_type 区分格式）。
- 退回专用工具的四种情况：①安全/权限/审计（生产库写操作）②屏蔽平台差异给更好反馈（grep/find 跨 Mac/Win/Linux）③使用频率极高 ④参数结构复杂（嵌套对象、多字段联合校验）。
- 四个决策维度：安全与权限、参数复杂度、变更频率（频繁变用 Skill 维护成本更低）、模型能力（强模型可用 Skill+通用执行器减工具数）。
- 让代码编排工具调用：通用执行器让 LLM 一次性生成脚本串联多工具，中间变量留执行环境，仅最终结果返回，token 消耗可降低约两个数量级（属第 5 章「代码作为通用 Agent 元能力」先声）。
- 工具描述的艺术。核心是让 LLM 知「什么时候用」而非「能做什么」。边界条件比能力更重要（多数失败根因是不知工具不能做什么）。要点：参数用具体例子（timestamp 用 2024-03-15T14:30:00Z、phone 用 +8613888888888）、描述返回值结构、注明执行代价（如「下载完整网页需 5-10 秒」）、附 1-5 个真实调用示例（准确率可从约 72% 提升到 90%）、频繁选错工具应优先检查描述而非换模型。
- 参数传递的保真性。反模式：①静默输入转换——如某工具把中文弯引号 " " 静默转为英文直引号导致匹配失败；②静默参数注入——某 IDE bash 工具自动给 git commit 附加参数致旧版报错。基础原则：模型感知到的世界与工具操作的世界之间不能存在系统性偏差，规范化须在描述中说明并明确返回告知。

### 4.3 工具生态：MCP 与 Skill Hub

- MCP：Anthropic 2024 末开放标准，客户端-服务器架构，JSON Schema 描述工具，支持本地 stdio 与远程 Streamable HTTP（早期 SSE 已弃用），三类原语 tools（可执行）/resources（只读数据）/prompts（模板）；生态价值「一次开发，处处可用」。
- Skill Hub：Skill 不需协议，一个装着 SKILL.md 的文件夹，分发靠注册表（Vercel skills.sh 的 npx skills add <owner>/<repo>、OpenClaw 的 ClawHub）。
- Token 成本差异：接入 MCP 服务器=运行时建连接，全部工具定义进入每次会话上下文；安装 Skill=拷文件夹，常驻仅 name+description，便宜一到两个数量级。
- 第三方能力的安全风险（以 MCP 为例）：①工具描述投毒（description 原样进上下文夹带指令，提示注入变种）②恶意/被劫持服务器（供应链攻击）③同名工具遮蔽 tool shadowing。缓解：审查描述、锁定版本拒静默更新、最小权限凭证、运行时 Sidecar 最后防线；第 5 章介绍致命三要素框架。Skill 警告：含实现代码可本地运行，危险系数比 MCP 大得多，需在隔离环境小心使用。

### 4.4 工具太多怎么办：层次化组织与主动工具发现

「能力形式」问形态，本问「一次让模型看见多少」。超百工具平铺占 token 击穿 KV Cache、先进 LLM 也易选错。三层渐进：

- 层次化组织与按需加载：MCP 扩张致工程问题（5 服务器数万 token 占 200K 窗口近三成）。Cursor 方案——工具描述同步文件夹，Agent 默认只见名称索引，需时查定义，A/B 测试 token 消耗减 46.9%。Pi Coding Agent 刻意不内置 MCP，优先封装带 README 的 CLI 由 Skills 按需加载。pi-mcp-adapter 折中：模型默认只见约 200 token 代理工具，经「搜索→查定义→调用」按需发现。层次化按信息源分搜索/读取/解析/查询四类；检索式预筛选（Anthropic 实验 Opus 4 准确率 49%→74%）。
- 模型原生的主动工具发现：MCP-Zero 代表——系统提示不预置 schema，Agent 生成结构化请求块（如「GitHub 服务器：搜索仓库」），经服务器级→工具级语义路由从数千候选匹配注入，约 2800 工具上比全量注入省约 98% token。工程等价：留 web_search/code_interpreter+「工具搜索工具」。动态加载需保持静态前缀稳定以命中 KV Cache（OpenAI 保 tool_search_output 原位置、Anthropic 内联 tool_reference）。实验 4-1（Qwen3-4B，120+ 工具）对照：全量注入超 50K token 致 4B 模型指令退化；实验组只留 web_search/code_interpreter/discover_tools，语义匹配返回 3-5 个候选 schema 追加到对话，准确率与完成率显著提升。
- Skills：把工具发现变成「按需查阅」：启动只看到薄目录（每个 skill 的 name+description，合计数百 token），当前上下文真正需要时再读 sub-skill 并顺引用往下读脚本。类比「没人会把整本工具书从头读到尾，而是顺着索引查词条」。专用工具要达到同样渐进披露须另建嵌入索引+检索元工具+API 原语，故 Skills 更现代省心。MCP 与 Skill Hub 并非互不相干——MCP 官方推动 skill 经 MCP 被发现和传递。

### 4.5 感知工具

设计在粒度、组织、输出格式多维权衡，常遇返回量远超处理能力，通用做法是在工具层集成第 2 章上下文感知压缩（超阈值如 10000 字符自动压缩）。

- 返回格式与分页：搜索返回结构化候选列表（标题/位置/摘要片段）而非全文；结果多时提供 cursor 分页参数，默认前若干条并注明总数与翻页方式。
- 读取类 offset/limit 与截断：read 类支持 offset/limit 按需读片段；静默截断危险——须明确标示「已显示第 1-200 行，共 5000 行，可用 offset 继续」，否则 Agent 误以为看全而误判。
- 只读性工程红利：结果可安全缓存、多调用可放心并行（同时读五文件、并发三搜索）；执行工具无此自由（顺序与副作用须严控）。
- 多模态输出形态：截图/图表/扫描件交模型前，选直接返回图像（保布局但耗 token）还是 OCR/解析转文本（精简但丢空间结构）；实践：纯文字用文本提取，布局敏感（UI/复杂表格/设计稿）保留图像。
- 实验 4-2（感知工具 MCP）：搜索/多模态理解/文件系统/公开数据源（天气·股价·Wikipedia·ArXiv 等免费 API）/私有数据源（日历·Notion 需授权）。多数基于免费开放 API，MCP 生态已有现成服务器；第 5 章将论证多数功能可用七个核心工具+Skill 覆盖。
- 多模态感知三条路径：
  - 原生多模态处理：能力上限最高，Vision Transformer（ViT）切块序列化与文本同处共享语义空间，自注意力平等对待跨模态 Token（如 Qwen-VL、LLaVA），可直接「看到」PDF 布局与图文空间关系。
  - 提取为文本 (Extract to Text)：先 OCR/转录转纯文本再入 LLM（如 GLM 5.2、DeepSeek V4 Flash 不支持原生多模态时的变通）。纯文本 PDF 比转图片省 token（一页文字几百 token vs 截图上千），但丢失版式/图表/图像信息。
  - 工具化多模态分析：主模型不支持多模态时，把分析封装为工具（analyze_image/analyze_pdf/analyze_audio 接受文件+自然语言问题返回描述），仅保留简短问答在上下文，避免多模态数据占 token，且工具内多模态模型无需强 Agent 能力、选型更自由。
- 实验 4-3（multimodal-agent demo.py 对比）：原生模式图表/布局最佳；提取文本模式纯文本文档成本效益最高但无法处理视觉查询；工具化模式交互灵活、低成本初步查询+按需高成本深析，但端到端深度理解不如原生。

### 4.6 执行工具

错误代价极高，需在能力开放与安全约束间平衡。

- 安全机制层次化：①输入验证（路径遍历 ../../etc/passwd、命令注入 ;/|、参数类型格式；快速失败不智能修正）②权限控制（文件限工作目录、命令黑名单 rm -rf //dd if=/dev/zero、API 配额）。注意黑名单只是最基础层，攻击者可变形式绕过，更健壮方案结合语义解析理解实际意图（第 5 章展开）。
- 提议者-审核者：应用于安全审查有两种机制。事前审批（Proposer 提议、独立 Reviewer 批准，如银行双签）：三要点——模型选不同家族但能力相近（如 Claude Opus 5 与 GPT-5.6 Sol 互审，引入认知多样性）、提示词底层规则一致但关注点差异（行动 vs 风险）、审批失败将拒绝理由作工具结果加入轨迹；可风险分级审批、不确定升级人工。事后验证：操作完成后模态切换检验（如文档渲染为视觉输出查排版、配置在沙盒实跑验生效）。表 4-2 对比（见第五节）。
- Sidecar 机制：与主模型流式输出并行、门控单次工具调用的轻量 LLM 安全校验（Claude Code 自动模式典型）。命名自微服务边车模式，审查的是行为非最终输出。示例：bash("rm -rf /tmp/data") → Sidecar 收结构化输入 {tool:"bash",command:"..."} 识别 rm -rf 判高风险拒绝并要求确认，数百毫秒内完成近乎无感。反直觉点：前文说「能力相差过大互审不可靠」，此处用轻量模型是因审查对象为简单分类问题，轻量足矣。连续多次拒绝转人工（第一章 Harness「纠正」实例）。还可并行构造/补充上下文（筛用户记忆、摘要长输出）。
- 自动验证与反馈闭环：「如果操作结果可被验证就应自动验证」——write_file 写入后立即跑 linter，输出解析为结构化错误列表返回，形成「执行-验证-反馈」闭环。
- 长输出截断与持久化：超阈值（如 200 行/10000 字符）只留头尾各 50 行，中间提示「[省略 N 行，完整输出已存 /tmp/...]」，引导用 read_file 读取。
- 沙盒隔离：通用执行工具须沙盒。误区澄清：Python venv 不是沙盒（只隔离包依赖，不隔离文件/网络/进程）。隔离强度递增：进程级（本地同权限）→容器（Docker 共享内核）→ microVM（Firecracker 独立内核）；容器/microVM 须设 CPU/内存/磁盘/网络上限。
- 可观测性：日志（时间/参数/结果/耗时）、审计追踪、性能指标、告警。
- 幂等性与取消语义：「此操作副作用到底发生没」。手段：①携带唯一标识去重 ②先查询后变更。非幂等操作（发邮件/转账）用「预检-确认」两段式（异源模型+专用检查提示预检，再执行，失败不可盲目重试须返回错误给主模型重规划）。
- 实验 4-4（执行工具 MCP）：文件写改（写入后自动 linter）、终端（超时/危险命令检测 rm/dd/curl|sh）、code_interpreter（沙盒/审批/长输出总结）、数据操作、外部对接（日历/GitHub PR/邮件/Webhook）、GUI（browser-use/Computer Use/Android World）。

### 4.7 协作工具

任务超单 Agent 边界时，把子任务委托给其他 Agent/人类再整合。

- 子 Agent 哲学：专业化分工，各自优化提示词/工具/知识库互不冲突。提示词四要素：角色定义清晰、上下文来源明确标注（[FROM_MAIN_AGENT]/[FROM_USER]/[TOOL_RESULT] 防提示注入）、任务边界界定、输出格式标准化。
- 协作机制原语：启动取消（spawn/cancel_subagent）、消息传递（send_message_to_subagent 双向）、发现（list_agents 列职责与状态，与 MCP tools/list 同一思路）。形态：同步/异步/流式/多轮。
- 人工介入 HITL：超时降级（「5 分钟无响应用保守策略」）、优先级队列（紧急多渠道通知）、反馈循环（批准/拒绝及理由进知识库或 Skill，高维偏好形成后训练数据）。
- 实验 4-5（协作工具 MCP）：子 Agent 管理（spawn/send/cancel/get_subagent_status）+ 人类协作（request_human_approval/input、send_im/email/slack 通知），要求设计智能协作策略、识别 HITL 时机、超时与多渠道通知。另附 active-tool-selection 项目（Agent 按需选工具组合）与 docker-compose.yml/DOCKER_DEPLOYMENT.md 容器化部署参考。

## 四、关键洞察与反直觉点

- 形态与披露是两个独立决策：专用工具 vs Skill 定每条常驻 token；披露策略定同时暴露多少条（skill 目录便宜一个数量级只让披露更宽松，不替你做披露选择）。
- 默认通用工具优于专用工具：利用 LLM 代码能力而非限制它。
- 工具描述中边界比能力更重要；频繁选错工具应优先查描述而非换模型，投入产出比远高于换更强模型。
- 模型感知与工具操作间不能有系统性偏差（静默转换/注入是大忌）。
- 代码编排工具调用 token 消耗可降约两个数量级。
- MCP 与暴露所有 MCP 工具是两个独立决策：后端可保留 MCP 兼容，前端仍用 CLI+Skills 渐进披露防 token 膨胀。
- 感知只读性红利：可缓存、可并行；执行工具无此自由。
- Sidecar 用轻量模型不矛盾：审查的是简单分类而非开放式思考。
- Python venv 不是沙盒（常见误区）。
- Skill 危险系数比 MCP 更大（含本地运行代码）。
- MCP 与 Skill Hub 并非互不相干：skill 可经 MCP 被发现和传递。

## 五、重要表格 / 数据 / 公式

### 表 4-1 五类工具的调用方向与作用对象

（见 4.1 节）

### 表 4-2 提议者-审核者机制 vs Sidecar 机制

| 维度 | 提议者-审核者 | Sidecar |
|------|--------------|---------|
| 执行时机 | 操作前（事前审批）或操作后（事后验证） | 与主模型流式输出并行，门控单次工具调用 |
| 审查对象 | 操作的合理性或结果 | 操作本身（工具调用） |
| 审查视角 | 独立模型审批、模态切换验证 | 安全性/可靠性校验 |
| 输入隔离 | 提议者与审查者看相似信息 | Sidecar 刻意隔离主模型自由文本 |
| 典型用途 | 不可逆操作审批、文档生成、配置修改 | 权限分类、记忆相关性判断、工具输出摘要 |

### 关键数据

- Cursor 按需加载：MCP token 消耗减 46.9%
- Anthropic 检索式预筛选：Opus 4 准确率 49%→74%
- MCP-Zero：约 2800 工具，比全量注入省约 98% token
- 工具调用加示例：准确率约 72%→90%
- 代码编排工具：token 降约两个数量级
- 上下文感知压缩阈值示例 10000 字符；长输出阈值 200 行/10000 字符，截取头尾各 50 行

## 六、配套实验清单

| 实验 | 内容 | 关键文件 |
|---|---|---|
| active-tool-discovery (4-1) | 主动工具发现（Qwen3-4B，120+ 工具） | 仅留 web_search/code_interpreter/discover_tools，语义匹配注入 schema |
| perception-tools (4-2) | 感知工具 MCP（搜索/多模态/文件/公开·私有数据源） | 无需 API Key 的多类感知服务器 |
| multimodal-agent (4-3) | 原生多模态 vs 提取文本 vs 工具化 三范式对比 | multimodal-agent/demo.py |
| execution-tools (4-4) | 执行工具 MCP（含 schema 校验、风险分类、结果验证） | python cli.py demo 离线调用起步 |
| collaboration-tools (4-5) | 协作工具 MCP（子 Agent、HITL、通知、定时器、管理员审批） | 子 Agent 管理 + 人类协作原语 |
| active-tool-selection (—) | Agent 按任务主动选工具组合 | 独立项目（非编号对照） |

另：chapter4/docker-compose.yml 与 DOCKER_DEPLOYMENT.md 提供 MCP 工具服务器容器化部署参考。4-1 至 4-5 可执行核心均通过真实运行，但私有数据授权/外部通知/真实邮箱门禁按证据诚实标 blocked，不以 mock 替代。

## 七、本章小结与思考题要点

**本章小结**：工具设计决定 Agent 能力上限。第一决策是能力表达形式——默认往通用端靠，仅安全权限/参数复杂/高频/平台差异四种情况退回专用工具；它与「一次让模型看见多少」独立。能力靠两条渠道分发：MCP 统一专用工具接入，Skill Hub 用包管理器分发 SKILL.md；二者都把引入成本压到一条命令，也扩大信任边界，须审查描述与版本、隔离凭证、保证模型看到参数与工具真正执行参数一致。工具增长到成百上千时，层次化组织、按需加载、主动发现与 Skills 依次接管，把「选哪个工具」变「查哪条资料」。本章展开五类中 Agent 主动调用的三类（感知控信息量、执行控风险、协作做分工）；事件触发与用户沟通由外部驱动或异步触达，放第 6 章。下一章回答：Agent 能否通过写代码创造工具？

**思考题要点**（共 4 题，均 ★★）：

1. MCP 将工具定义从框架解耦，但流式输出/双向通信/有状态会话难在标准中表达，MCP 未来最需扩展什么能力？
2. 多 MCP 服务器功能重叠，Agent 面对同名略异工具如何选择？能否感知并利用「一个返回摘要、一个返回全文」的差异？
3. 「执行-验证-反馈」闭环还能用于哪些工具场景？是否存在验证成本/风险超操作本身而不适用的情况？
4. 「工具爆炸」除主动工具发现还有哪些方案？参考人类专家面对大量工具时的策略。

## 八、术语表

| 术语 | 含义 |
|---|---|
| ACI | Agent-Computer Interface，工具对应 Agent 目标而非底层 API |
| 五类工具 | 感知/执行/协作/事件触发/用户沟通 |
| 专用工具 / 通用执行器 / Skill | 结构化函数调用 / bash·code_interpreter / 自然语言 SKILL.md |
| 静默输入转换 / 静默参数注入 | 工具偷偷修正或附加参数，制造系统性偏差 |
| MCP | Model Context Protocol，统一模型与工具通信的开放标准 |
| Skill Hub | 用注册表分发 SKILL.md 文件夹的能力分发方式 |
| 按需加载 / 主动工具发现 | 只暴露索引 / Agent 主动声明能力缺口动态匹配注入 |
| MCP-Zero | 系统提示不预置 schema、语义路由匹配注入的主动发现工作 |
| Proposer-Reviewer | 异源模型双签的事前审批与事后验证安全审查范式 |
| Sidecar | 与主模型并行、门控单次工具调用的轻量安全校验 |
| HITL | Human-In-The-Loop，人在回路审核 |
| 幂等性 | 同操作执行多次对外部世界影响相同，可安全重试 |
| 致命三要素 | 第 5 章将介绍的提示注入风险评估框架（Simon Willison） |

## 九、与其他章节的关联

- 第 1 章：提议者-审核者 Proposer-Reviewer 范式、Harness「纠正」功能（Sidecar 连续拒绝转人工即其实例）、动态轨迹。
- 第 2 章：上下文感知压缩（感知工具层集成，阈值 10000 字符）、提示注入与来源标记（工具描述投毒、子 Agent 上下文标注防注入）。
- 第 5 章：代码作为通用 Agent 元能力（「让代码编排工具调用」先声）、致命三要素框架、语义解析命令意图。
- 第 6 章：事件触发工具与用户沟通工具（异步运行时）、事件驱动架构、业务状态。
- 第 10 章：多 Agent 架构（协作工具基础，spawn_subagent 等）。
