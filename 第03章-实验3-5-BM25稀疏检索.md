# 第 3 章 实验 3-5：从零实现 BM25 稀疏检索

来源：《深入理解 AI Agent：设计原理与工程实践》（bojieli/ai-agent-book，Apache-2.0）
配套实验：chapter3/sparse-embedding ｜ 难度：🟢 入门级（零依赖）｜ 状态：✅ 已通过验收
运行日期：2026-09-22

## 一、实验目标

从零实现倒排索引 + BM25 打分，不借助任何第三方检索库，验证三件事：

1. 自研 BM25 的数学实现是否正确（手算 vs 引擎，误差需 ≤ 1e-12）
2. 精确关键词查询能否被完整召回
3. 同义词查询会如何失败——以及为什么

**这是第三章 12 个实验里唯一零第三方依赖的**：只 import 标准库（`math` / `json` / `argparse` / `re` / `collections`），不需要 torch、不需要下模型、不需要 API key。官方推荐阅读顺序（`sparse-embedding` → `retrieval-pipeline` → `user-memory`）的第一步。

## 二、环境与准备

| 项 | 值 |
|---|---|
| 环境 | conda `agentbook`（`D:\ruanjian\MiniConda\miniconda\envs\agentbook`） |
| Python | 3.12.14（Anaconda 打包） |
| 平台 | Windows-11-10.0.26200-SP0 |
| 第三方依赖 | **无** |
| 工作目录 | `E:\python-study\ai-agent-book\book-sparse2\chapter3\sparse-embedding` |

### 取文件的坑

仓库用的是稀疏克隆，第三章默认不在工作区。取文件：

```cmd
cd /d E:\python-study\ai-agent-book\book-sparse2
git sparse-checkout add chapter3/sparse-embedding
git checkout
```

注意用 `add` 而不是 `set`——`set` 是**覆盖**，会把已有的 chapter4 踢出去。

**踩坑**：`benchmark.py` 依赖 `chapter3/experiment_utils.py`（在上一级目录），但 `sparse-checkout add` 只取子目录，这个文件不会跟着下来。跑 `benchmark.py` 会报 `ModuleNotFoundError: No module named 'experiment_utils'`。补上：

```cmd
git show HEAD:chapter3/experiment_utils.py > chapter3/experiment_utils.py
```

`benchmark.py` 第 18-20 行自己做了 `sys.path.insert(0, CHAPTER_DIR)`，所以文件放对位置就能 import，不用改代码。

## 三、四条命令与结果

### 3.1 `python cli.py --eval` —— 检索质量评测

内置语料 10 篇英文文档，内置标注 5 条查询。输出：

| 查询 | 相关文档 | 召回排序 | recall@5 | precision@5 | RR |
|---|---|---|---|---|---|
| model distillation | [doc_3, doc_4] | [doc_3, doc_4] | 1.00 | 1.00 | 1.00 |
| HTTP 404 error | [doc_6] | [doc_6, doc_9] | 1.00 | 0.50 | 1.00 |
| XK9-2B4-7Q1 | [doc_9] | [doc_9] | 1.00 | 1.00 | 1.00 |
| BM25 ranking function | [doc_5] | [doc_5] | 1.00 | 1.00 | 1.00 |
| cat | [doc_7, doc_8] | **[]** | **0.00** | 0.00 | 0.00 |
| **宏平均** | | | **0.800** | **0.700** | **0.800** |

关键：前 4 条精确关键词查询全部完美召回，`cat` **一条都没召回**。

注意 `HTTP 404 error` 的 precision@5 = 0.50——只标了 1 篇相关文档，但召回了 2 篇，所以 1/2。precision 的分母是「你返回了几篇」，不是「总共有几篇相关」。

### 3.2 `python cli.py --explain -q "BM25 ranking function"` —— 打分机制解剖

```
#1 doc_5  score=5.6263  BM25 Ranking
   命中词: ['BM25', 'ranking', 'function']
   'BM25':     TF=1, 文档长度=11词, IDF=1.8458, BM25贡献=1.8754
   'ranking':  TF=1, 文档长度=11词, IDF=1.8458, BM25贡献=1.8754
   'function': TF=1, 文档长度=11词, IDF=1.8458, BM25贡献=1.8754
```

三个词贡献各 1.8754，合计 5.6263。

**手算验证**：N=10，df=1（"BM25" 全语料只出现在 doc_5），tf=1，dl=11，avgdl=11.40，k1=1.5，b=0.75

- IDF = ln((10−1+0.5)/(1+0.5)) = ln(9.5/1.5) = ln(6.3333) = **1.8458** ✓
- 归一化项 = k1×(1−b+b·dl/avgdl) = 1.5×(0.25+0.75×11/11.40) = 1.5×0.9737 = 1.4605
- 分子 = tf×(k1+1) = 1×2.5 = 2.5
- 贡献 = 1.8458 × 2.5 / (1+1.4605) = 4.6145/2.4605 = **1.8754** ✓

### 3.3 `python test_engine.py` —— 引擎正确性测试

六个测试模块全过：文本处理、倒排索引、BM25 打分、搜索引擎、边界情况、排序质量。

### 3.4 `python benchmark.py` —— 官方六项验收 gate

```
hand score error: 0
exact recall@5: 1.000
synonym recall@5: 0.000
evidence: ...\validation\runs\20260922T144740Z-3_5-8a911759
```

六项 acceptance 全 `true`：

| gate | 含义 | 结果 |
|---|---|---|
| `uses_from_scratch_engine` | 用的是自研引擎，不是库替代 | true |
| `hand_score_matches` | 手算得分与引擎一致 | true |
| `inverted_index_df_matches` | 倒排索引记录的 df 等于手算 df | true |
| `all_exact_keyword_queries_recalled` | 精确关键词查询全召回 | true |
| `synonym_only_failure_observed` | **同义词失败被复现** | true |
| `transparent_tf_idf_scores_retained` | 每条结果保留 TF/IDF 分项 | true |

**注意第 5 项**：gate 要求同义词召回率**必须等于 0**——这是个「必须复现失败」的验收。失败本身是被验证的对象。

## 四、三个核心洞察

### 4.1 IDF 是「稀有度奖励」

`HTTP 404 error` 查询的逐项得分最能说明问题：

| 词项 | df | IDF | 贡献 |
|---|---|---|---|
| HTTP | 1 | 1.8458 | 1.8754 |
| 404 | 1 | 1.8458 | 1.8754 |
| error | 2 | 1.2238 | 1.2434 |

同样 tf=1，`HTTP`/`404` 全语料只出现 1 次，IDF 高达 1.8458；`error` 出现 2 次，IDF 掉到 1.2238。**这也是为什么专有名词、错误码、产品型号这类查询天然得分高。**

### 4.2 IDF 负值截断——BM25 的隐藏修正

`test_engine.py` 的最后一段暴露了这个设计：

```
IDF for 'machine':  N=10, df=6, raw_idf=-0.3677, scoring_idf=0.0000
IDF for 'learning': N=10, df=9, raw_idf=-1.8458, scoring_idf=0.0000
```

查询 `machine learning`，**9 篇文档得分全是 0.0000**。

原因：公式 `ln((N−df+0.5)/(df+0.5))`，当 `df` 超过文档总数一半时分子小于分母，`ln` 出**负数**。负 IDF 会让「越常见的词越扣分」，这显然荒谬。所以代码做了处理：`raw_idf` 保留原值（可为负），参与打分的 `scoring_idf` **截断到 0**（`max(0, raw_idf)`）。

后果就是：**查两个高频词的组合 ≈ 没有排序**。上面 `machine learning` 的结果是按文档内部 ID 顺序排的，纯属默认序。

### 4.3 `cat` 查询失效——稀疏检索的天花板

日志写得很清楚：

```
Term 'cat' appears in 0 documents
Found 0 candidate documents
Returning top 0 results
```

标注里 `cat` 的相关文档是 doc_7（"A cute **kitten** chased a ball of yarn..."）和 doc_8（"The **feline** predator stalked its prey..."）——**两篇讲猫的文档，一个 "cat" 都没写**。

"cat" 在整个语料里 df=0，IDF 再高也没用，因为它在任何文档里都不出现，所有文档得分都是 0 → 被过滤 → 候选集为空。**这不是排序排得不好，是根本没法匹配。**

这就是稀疏检索的数学本质：**只能在字面相同的词项上匹配**。要救这类查询，必须换稠密向量（语义相似度）——这正是实验 3-6 `retrieval-pipeline` 要解决的问题。

### 附带发现：停用词表不完美

`test_engine.py` 开头：

```
Tokens (with stop words):    ['the','quick','brown','fox','jumps','over','the','lazy','dog']  → 9 个
Tokens (without stop words): ['quick','brown','fox','jumps','over','lazy','dog']             → 7 个
```

`the` 去掉了两个，但 `over` 留下了。停用词表有 61 个词，但这类介词漏收很正常——**停用词表永远不完美**。

## 五、验收证据

### 产出位置

```
E:\python-study\ai-agent-book\book-sparse2\chapter3\sparse-embedding\validation\
├── latest.json                                  ← 指向本次运行
└── runs\
    └── 20260922T144740Z-3_5-8a911759\
        ├── evidence.json                        ← 完整实验数据
        ├── receipts.json                        ← 无凭据记录
        └── manifest.json                        ← 哈希清单
```

### 证据里记录了什么

- **provenance**：`git_revision: 73ffc0d7...`、`python: 3.12.14 (Anaconda)`、`platform: Windows-11-10.0.26200-SP0`、`credential_presence` 全部 false
- **hand_calculation**：期望值 1.1373125642781259、引擎值 1.1373125642781259、`absolute_error: 0.0`、容差 1e-12
- **index_statistics**：`total_documents: 10`、`unique_terms: 98`、`total_terms: 114`、`average_document_length: 11.4`
- **queries**：5 条查询的逐条排名、得分、命中词项、TF
- **inputs**：三个源文件的 SHA-256 + 字节数（`benchmark.py` 7229B / `bm25_engine.py` 21278B / `cli.py` 16345B）

`credential_presence` 只记录「环境变量存不存在」，**不记录值**——不会泄露任何密钥。

### 与官方证据对比

官方在 `validation/runs/20260729T183232Z-3_5-f9f70b37/` 留了一份 macOS 上跑的证据：

| 字段 | 本次运行 | 官方运行 |
|---|---|---|
| status | passed | passed |
| absolute_error | 0.0 | 0.0 |
| git_revision | `73ffc0d7` | `4a7f37cf` |
| platform | Windows-11 / Py 3.12.14 | macOS-26.3 / Py 3.11.4 |
| build_latency_ms | 18.164 | 1.668 |
| unique_terms | 98 | 98 |
| avgdl | 11.40 | 11.40 |
| 5 条查询得分 | 3.1216/2.2197/4.9943/1.9538/5.6263 | 完全相同 |

**结论：检索质量跨平台完全一致，性能差约 10 倍。** 建索引 18ms vs 1.7ms 在 10 篇文档的量级上毫无实际意义，但也说明：**证据里的延迟数字只对生成它的机器有意义**，跨机器比延迟没意义。

`git_revision` 不同说明拉的是更新的 commit，`inputs` 里的哈希能证明跑的是哪个版本。

## 六、命令速查

```cmd
cd /d E:\python-study\ai-agent-book\book-sparse2\chapter3\sparse-embedding

python cli.py --eval                                  # 检索质量评测
python cli.py --explain -q "BM25 ranking function"    # 打分机制解剖
python cli.py -q "cat" -k 5                           # 单条查询
python cli.py -q "cat" --method splade                # 试 SPLADE（需 torch，未装）
python test_engine.py                                 # 引擎正确性测试
python benchmark.py                                   # 官方六项验收

python benchmark.py > benchmark_output.txt 2>&1       # 输出太长时落盘再读
```

`cli.py` 参数：`-q/--query`、`-c/--corpus`、`-m/--method {bm25,splade}`、`-k/--top-k`、`-o/--output`、`--eval`、`--labels`、`--explain`、`--k1`、`-b/--b`、`-v/--verbose`

## 七、与第三章正文的对应

正文 3.2 节「稀疏嵌入」讲到：*「实验 3-5 从零实现 BM25 + 倒排索引，运行日志显示『蒸馏』虽 TF 低但 IDF 高、贡献反超『模型』」*。

本次运行验证了这个现象，但更精确的表述是：

- `model distillation` 查询里，doc_3 的 `model` TF=3 贡献 1.9705，`distillation` TF=1 贡献 1.1511——**同一篇文档内**，TF 高的词贡献更大
- 但跨查询看，`distillation` 的 IDF(1.2238) 高于常见词，这才是它能进候选集的原因
- 真正体现「低 TF 高 IDF 反超」的是 `HTTP 404 error`：`404` 只出现 1 次（TF=1）但贡献 1.8754；`error` 出现 2 次（TF=1）贡献只有 1.2434

正文提到的 `doc_1=3.67 > doc_7=1.68 > doc_5=1.22 > doc_3=0.82` 那组数字来自正文的另一个示例语料，与本次内置语料不同，不能直接对照。

## 八、下一步

**实验 3-6 `retrieval-pipeline`**（稠密 + 稀疏 + 神经重排序）。

门槛比 3-5 高一个量级：

- 需装：`torch`（Windows CPU 约 200MB）、`transformers`、`sentence-transformers`、`rank-bm25`
- 首次运行需下载：`all-MiniLM-L6-v2`（~90MB）、`bge-reranker-base`（~1.1GB）
- `config.py` 里 `RerankerConfig.device` 硬编码 `"mps"`（Mac 专用），Windows 需改
- 环境里 `pip install -r` 会被守卫中断，需走 `pip download --no-deps` + 手动解包

推荐的推进方式：先用 `python evaluate.py --no-dense --no-rerank`（纯 BM25，只需 `rank-bm25`）探路，确认流程通了再装模型上完整版。

预期对照——官方 README 已给出复现结果：

| Stage | Recall@3 | MRR | nDCG@3 |
|---|---|---|---|
| BM25 (sparse) | 0.9000 | 0.8500 | 0.8631 |
| Dense | 1.0000 | 0.9000 | 0.9262 |
| Hybrid-RRF | **1.0000** | **1.0000** | **1.0000** |
| Hybrid-Weighted | 1.0000 | 0.9500 | 0.9631 |
| Hybrid-RRF+Rerank | 1.0000 | 0.9500 | 0.9631 |

看点：BM25 单独用漏掉 10%，稠密单独用漏掉 MRR 的 10%，**混合后满分**——这正是 3-5 揭示的「稀疏有天花板」的解法。
