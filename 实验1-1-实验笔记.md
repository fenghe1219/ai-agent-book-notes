# 实验 1-1：上下文的关键作用 —— 实验笔记

日期：2026-09-20

---

## 一、实验环境

- 实验代码：`E:\ai-agent-book\chapter1\context\`
- Python 环境：conda 环境 `agentbook`（Python 3.12.14）
- 模型：DeepSeek（`deepseek-v4-flash`），API key 在 `chapter1\context\.env`（不要外传）
- 每次运行的准备动作：

```
conda activate agentbook
cd /d E:\ai-agent-book\chapter1\context
```

---

## 二、目录结构（以及为什么要这样摆）

```
E:\ai-agent-book\
├── agentbook\          ← 共享包：统一管理各家大模型的接入（DeepSeek/Kimi/通义等）
├── pyproject.toml      ← 安装说明书（pip install -e ".[ch1]" 靠它）
├── README.md
└── chapter1\
    └── context\        ← 实验 1-1 的代码
        ├── .env        ← API key 所在
        ├── main.py     ← 实验入口
        ├── agent.py    ← Agent 主逻辑
        └── ...
```

两个关键点：
1. `agentbook` 是仓库自带的本地包，必须以"可编辑安装"方式装进 conda 环境，命令在仓库根目录执行：`pip install -e ".[ch1]"`
2. 实验代码必须放在 `chapter1\context\` 两层结构下（代码里的兜底逻辑写死了这个层级）

---

## 三、今天跑通了什么

运行命令：

```
python main.py --mode single --task "把 1000 美元换算成欧元、英镑、日元并求平均值" --provider deepseek
```

结果文件：`task_result_full.json`，状态 **success = true**，3 轮循环，4 次工具调用。

| 轮次 | 模型做了什么 | 工具调用 | 输入 tokens |
|------|-------------|---------|------------|
| 1 | 思考：三种货币各换算一次 | `convert_currency` × 3（一次响应里并发） | 748 |
| 2 | 拿到 €920 / £790 / ¥149,500 | `calculate`：(920+790+149500)/3 | 1,298 |
| 3 | 不再调工具，组织答案 | — | 1,444 |

最终答案：€920.00、£790.00、¥149,500，平均值 ≈ **50,403.33**。

模型甚至还主动提醒：三种货币金额直接求平均，在经济学上意义有限——推理质量不错。

**核心观察**：输入 tokens 逐轮变长（748 → 1,298 → 1,444）。上下文在滚动累积，这正是书里第 2 章"上下文工程"要解决的问题——我已在亲手验证它。

---

## 四、怎么看实验输出（日志阅读法）

1. **终端日志 = 行车记录仪**。`INFO - Executing tool...` 是流水账，扫一眼即可，不用逐行读。
2. **ERROR = 单次工具调用失败，不等于实验失败**。Agent 的设计就是失败后自己绕路重试。判断标准只看最后的 `success` 和最终答案。
3. **成绩单 = 结果文件**：
   - 单任务：`task_result_full.json`（含每轮请求/响应/工具调用/tokens 的完整记录）
   - 消融实验：跑完会打印对比矩阵（每种模式的成败、迭代次数、工具调用数）+ 图表

---

## 五、踩坑记录（照抄即可绕开）

| 坑 | 原因 | 解法 |
|----|------|------|
| `No module named 'agentbook'` | agentbook 是仓库根的本地包，只下载 context 文件夹时没有它 | 仓库根放 `agentbook\` + `pyproject.toml`，在根目录执行 `pip install -e ".[ch1]"` |
| 实验目录少一层报错 | 代码兜底逻辑要求 `chapter1/context` 两层结构 | 把 context 移入 `chapter1\` 文件夹 |
| `cd E:\...` 没反应 | 跨盘符切换需要 `/d` 参数 | `cd /d E:\ai-agent-book` |
| 默认任务里 PDF 一直报 SSL 错 | 任务内嵌了 `raw.githubusercontent.com` 的 PDF 链接，国内连不上 | 忽略即可：5 个地区的金额直接写在任务文字里，计算不受影响 |
| 运行时报 provider 错 | main.py 默认用 siliconflow，而我的 key 是 DeepSeek 的 | 命令里始终加 `--provider deepseek` |

---

## 六、概念对照（实验 ↔ 书本）

- **Agent = LLM + 上下文 + 工具**：这次运行里，LLM 是 deepseek-v4-flash；上下文是 system 提示 + 任务描述 + 历史轮次；工具是 `convert_currency` / `calculate` / `parse_pdf` / `code_interpreter`。
- **上下文膨胀**：tokens 748 → 1,444，第 2 章的主题。历史轮次（含工具结果）每轮都塞回去，所以越滚越大。
- **实验主旨（消融）**：把上下文故意拆掉一块——去掉历史（no_history）、去掉推理（no_reasoning）、去掉工具调用（no_tool_calls）、去掉工具结果（no_tool_results）——看 Agent 能力掉多少，从而证明"上下文决定 Agent 的能力上限"。

---

## 七、完整消融结果（2026-09-20 18:20 完成）

命令：`python main.py --mode ablation --provider deepseek`

全程约 28 分钟。中途大量红色 ERROR（PDF 链接被墙）属预期；Ctrl+C 中断被程序吞掉继续跑，反而跑完了——因祸得福。

结果文件（都在 `chapter1\context\`）：`ablation_results.json`、`ablation_study_results.png`、`ablation_study_report.md`

| 模式 | 轮数 | 工具调用 | 结果 | 发生了什么 |
|------|------|---------|------|-----------|
| full（基线） | 8 | 16 | 成功 | 产出完整的财务分析报告，还部分读到了 PDF 内容 |
| no_history（无历史） | 10（顶格） | **50** | 失败 | 失忆 → 疯狂重复调工具 → 撞上 10 轮上限被掐断，没有产出答案 |
| no_reasoning（无推理） | — | — | 未能运行 | 书里代码自身的 bug（`name 'openai' is not defined`），与环境无关，跳过 |
| no_tool_calls（不能调工具） | 1 | 0 | 表面完成 | 模型只能在文本里"假装"调工具（DeepSeek 内部标记 DSML 都漏出来了），实际什么都没做成 |
| no_tool_results（工具无结果） | 5 | 10 | **最危险** | 看似成功交卷，但 grounding 检查判定 ungrounded——**13 个数字全是模型脑补的**汇率和金额 |

### 消融结论（实验 1-1 的核心答案）

上下文的三块料，各管一摊：

- **历史**管"不重复"：拿掉 → 失忆循环，工具调用从 16 次暴涨到 50 次，任务失败
- **工具调用**管"能动手"：拿掉 → 只能说不能做，输出假的调用指令文本
- **工具结果**管"不编造"：拿掉 → 最可怕：表面成功，实际幻觉

**最重要的教训**：最危险的失败不是报错，而是 no_tool_results 那种"自信地交了一份编造的答卷"。报错你会看见，幻觉你不会。这就是为什么生产系统要给 Agent 配 grounding/事实校验层。

### 过程小插曲（记下来以后少踩坑）

- Windows 终端 QuickEdit：鼠标点选文字会暂停程序输出（看似死机），按 Esc 恢复
- 运行中的程序不能输入命令，回到提示符才行
- jsdelivr 镜像在国内时好时坏，full 模式侥幸部分拿到了 PDF 内容

---

## 八、下一步

1. （可选）重跑快速对照：`python main.py --mode ablation --provider deepseek --ablation-modes full no_history`
2. 清理失败克隆残留：`rmdir /s /q E:\ai-agent-book\book-repo`
3. 存档：本笔记 + `ablation_study_results.png`
